---
- hosts: webservers
  become: True
  tasks:
    - name: start docker
      service:
        name: docker
        state: started
        enabled: yes

    - name: Start the container
      docker_container:
        name: webapp
        image: "the1amit/webapp"
        state: started
        ports:
          - "http://vmpl1000.eastus.cloudapp.azure.com:6500:8080"
